console.log """
  #{pkg.name} #{pkg.version}

  Usage: #{pkg.name} [options] [command]

  Clay is a program that runs on the terminal to build javascript apps for Salesforce.

  Commands:

    prepare        Creates the project folder and installs all dependencies

  Options:

    -l,    run in debug mode, showing all console output
    -h,    output usage information
    -v,    output the version number
    -u,    Updates your 3VOT Profile


  """