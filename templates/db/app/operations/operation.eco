command = require('../commands/')

log = require('3vot-db/logger').getLogger('_operation')

#App Must have developer, user_name, public_dev_key or private_dev_key

execute= ( object ) ->
  # default and update

  object = command(object)
  return object
  

module.exports = execute;